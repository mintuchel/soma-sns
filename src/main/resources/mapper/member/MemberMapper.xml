<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.soma.mini_sns.domain.member.mapper.MemberMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id"
            parameterType="com.soma.mini_sns.domain.member.entity.Member">
        INSERT INTO members (name, password, email, created_at, updated_at)
        VALUES (#{name}, #{password}, #{email}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <select id="existsByEmail" parameterType="string" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM members WHERE email = #{email})
    </select>

    <select id="existsByName" parameterType="string" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM members WHERE name = #{name})
    </select>

    <select id="findById" parameterType="long"
            resultType="com.soma.mini_sns.domain.member.dto.response.MemberInfoResponse">
        SELECT id, name, email
        FROM members
        WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <select id="findByEmail" parameterType="string"
            resultType="com.soma.mini_sns.domain.member.dto.response.MemberInfoResponse">
        SELECT id, name, email
        FROM members
        WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="findByEmailWithPassword" parameterType="string"
            resultType="com.soma.mini_sns.domain.member.entity.Member">
        SELECT id, name, password, email,
               created_at, updated_at, deleted_at
        FROM members
        WHERE email = #{email} AND deleted_at IS NULL
    </select>

</mapper>
